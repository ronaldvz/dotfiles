# Receiving options.
set imap_user = $my_user_personal
set imap_pass = $my_pw_personal 
set folder = $my_imap_personal 
set spoolfile="+Inbox"
set imap_check_subscribed
unset record
set postponed = +[Gmail]/Drafts

mailboxes +ronaldvanzuijlen-gmail.com/INBOX \
          +ronaldvanzuijlen-gmail.com/Sent Mail \

# Sending options.
set smtp_url = $my_smtp_personal
set ssl_starttls = yes

# Allow Mutt to open new imap connection automatically
unset imap_passive

# Keep IMAP connection alive by polling
set imap_keepalive = 300
